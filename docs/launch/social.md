# Twitter / LinkedIn

Lead with the diff. One thread.

1. Every SEO tool tells you what's wrong. Agent Sean fixes it.
2. [demo: finding → diff → Revert]
3. `npx agentsean` — loopback dashboard, freeze switch, 2 pages/day cap that you cannot raise.
4. Not a fork of OpenSEO. They proved OSS SEO can exist. Sean writes to the source of truth.
5. github.com/seziro-team/agentsean

Do not claim a ranking lift. Evidence tier E is the honest default on a small site.
